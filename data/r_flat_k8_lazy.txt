Flat-engine lazy k8 tier (--zpdb8) — E1-E4 feasibility program (2026-07-30)
===========================================================================

Goal: decide whether max(cWD, k8) can beat cWD-only for the R >= 152 run
(exhaust-150), now that the flat engine made cWD 1.7x faster than the recursive
engine the recorded k8 results were measured on.

E0 (from records, no runs)
--------------------------
Threshold-level node-cut ratios, k8-stack tree vs cWD-only tree:
  144: 1.57x   146: 2.03x   148: 3.03x (r_k8lazy_ex148.txt vs §8b)  ~1.5x/rung
The recursive 3-tier TIED cWD-only at 148 in wall time (5630.91 vs 5633 s).
Budget for the whole k8 layer = cwd_ns x (cut - 1) per built node:
  ~22 ns @144, ~39 @146, ~77 @148, ~130 @150(extrap).

E1 (by code inspection — decisive without a bench)
--------------------------------------------------
The zbins are 1-bit DIFFERENTIAL tables: absolute values exist only via
ZPatternDb::cold_lookup, which descends the abstract graph one step per unit of
h — O(h) ranks+probes, tens of us for k8-scale h. So a from-scratch consult
design is dead on arrival (this is §8u's retraction, sharpened), and no value
cache can rescue it (a miss costs us). The only viable design is incremental:
maintain (ProjectedState, h) per group per view, diff_lookup O(1) on cost-1
edges. Design A' (flat): consult = advance-by-one-move from the parent's slot,
done ONLY for children that pass cWD's f-test. Every entered node has a current
slot, so there is NEVER a catch-up — the entire recursive LazyMaxInc deferral
machinery (8% of samples, §8l) dissolves into the arena invariant.

E2 (built; node-identity gates)
-------------------------------
`solve24 --zpdb8`: mmap pdb24_k8_{a,b,c}.zbin (32.8 GB), consult at
cWD-survivors, h = max(sum normal, sum reflected), both drivers.

Node identity vs the recorded recursive 2-tier trees (§8j):
  threshold 146:  8,539,130,554  EXACT (sequential AND parallel)
  threshold 144:  269,180,917 vs recorded 269,180,930 — Δ = 13 (5e-8),
                  ours SMALLER (stronger tree), confined to the first
                  iteration; the 146 iteration matches bit-for-bit, which rules
                  out any systematic h error. Attributed to a first-iteration
                  convention quirk of the deleted lazy driver (its deferred
                  values used a Lipschitz bound v-k-1; deferred moves popped
                  before catch-up never saw true k8). Not chased further: 8.5B
                  matching decisions is the strongest available gate.

v1 consult: copy 306B slot + 6 apply_in_place slides + 2 rank + 2 diff_lookup.
v2 consult: the projected-edge law (cost-0 slides leave index and h unchanged)
means the four cost-0 group-views need not be slid AT ALL. Their stored blank
goes stale; healed via set_blank_pos immediately before that group-view's next
cost-1 apply+rank. Sound because rank reads pattern-tile positions, the blank,
and cells[] only at occupied pattern cells. Reflected view needs no
transpose_move: sigma-image cells + TAU-relabelled tile pick its cost-1 group.
Gotcha found by panic: the engine's boards are goal-CODED (H1), so the moved
"tile" is a code; TILE_OF_CODE decodes it for the PDB machinery.
v2 gated EXACT on both counts.

Measured, R, exhaust-146 threshold alone (this session, AC):
                          seq            W=8
  cWD-only                675.4 s        91.5 s
  2-tier v1              1047.1 s       148.7 s
  2-tier v2                 —           133.7 s  (63.9 Mn/s; -10.1% vs v1)

Per-node W=8 CPU at 146: cWD 42.9 ns; 2-tier v2 125.2 ns. The k8 layer costs
~82 ns/built-node amortized — vs the 39 ns break-even at 146 (LOSES 1.46x, as
E0 predicted) and ~115 ns at 148 (dead heat predicted; E3 racing now).

E4 (memory, measured mid-run)
-----------------------------
37.8 GB mapped (32.8 k8 + 4.4 cWD HashMap + 0.6 wd24) on the 34 GB box:
RSS plateaus at 19.9 GB, ~165 MB free — the k8 working set does NOT fit and
re-faults clean pages continuously. Parallel per-node contention at 146 is
+13.6% (2-tier) vs +7.7% (cWD-only, §8y). This is the structural headwind: k8
probes are exactly the scattered access §8y's memory term punishes.

E3: exhaust-148 race at W=8 — IN FLIGHT (2-tier then cWD-only).

E3 — the exhaust-148 race, W=8, back-to-back (2026-07-30, AC)
-------------------------------------------------------------
  engine       thr-148 nodes        thr-148 time    rate        total
  2-tier v2    193,311,603,634      3254.78 s       59.4 Mn/s   proves R>=150
  cWD-only     577,673,506,215      3256.50 s      177.4 Mn/s   proves R>=150

A DEAD HEAT: 0.05%. The recursive engines also tied at 148 (5630.91 vs 5633 s,
r_k8lazy_ex148.txt vs §8b) — both engines got ~1.73x faster and the §8k
iso-time crossing stayed planted at exhaust-148.

Free validation: the flat cWD-only 148 tree reproduces §8b's recorded count
EXACTLY (595,862,979,851 total), extending node identity to 600 B scale.
Measured cut ratio at 148: 577.67/193.31 = 2.99x (E0 estimated 3.03 from the
3-tier files). Depth degradation 146->148 is the same for both engines (-8.3%
vs -7.9%), so the E4 paging steady-state does not worsen relatively with depth.

Verdict for R >= 152 (exhaust-150)
----------------------------------
Cut-ratio ladder: 1.57 -> 2.03 -> 2.99 (x1.29, x1.47 per rung, rising).
Break-even at 150 requires the ratio to stop growing ENTIRELY (2.99 again);
the trend continuing gives cut ~4.4x and

  cWD-only @150:  ~16.8 T nodes / ~163 Mn/s  ~= 28.6 h
  2-tier   @150:  ~3.8 T nodes  / ~54.5 Mn/s ~= 19.3 h   (~33% faster)

RECOMMENDATION: run the R >= 152 attempt with `--parallel --zpdb8`. Projected
~19 h vs ~29 h; the projection's only soft spot is the unmeasured 150 cut
ratio, and every measured rung has moved it further in k8's favour.

Levers not yet pulled (would convert the 148 tie into a win and widen 150):
  - memoize (idx -> h) per worker to cut the 2 diff-probe faults (the tables
    oversubscribe RAM; a small cache attacks refaults the way CACHE_BITS did);
  - slot sharing (k8_at indices) to kill the 306 B copy;
  - W=12 (the +21% E-core option, declined for interactive use).

Memo-cache sizing measurement (k8-probe-locality feature, 2026-07-30)
---------------------------------------------------------------------
Method: mirror of the cWD probe-cache sizing, but simulating the candidate
structure ITSELF — an (idx -> h) direct-mapped memo, key = idx*3+table, same
multiply-shift index as ProbeCache — online during a budgeted sequential run
(--zpdb8 --max-nodes 1.8B: all of 144 plus ~1.5B nodes of the 146 pass; ~9% of
the stream is the 144 regime). Hooked at k8_child's two diff_lookup sites; seed
cold_lookups excluded. An (idx -> h) hit is sound path-independently because
diff_lookup's old_h argument only disambiguates the 1-bit encoding — h is a
function of idx alone. NOTE: the memo key is idx, which is computed by rank —
so a hit skips the TABLE TOUCH only, not the rank.

  probes: 2,776,598,476 (1.55/node; tables a/b/c 36%/31%/33%)
  distinct 16 KiB pages: a 2.86 GB, b 2.26 GB, c 3.78 GB = 8.9 GB in one slice

  bits  /worker   hit      misses/node
  14    128 KB    63.76%   0.56
  16    512 KB    73.49%   0.41
  18      2 MB    81.29%   0.29     (cWD probe cache at ~this size: 98.6%)
  20      8 MB    87.31%   0.20
  22     32 MB    91.87%   0.13
  24    128 MB    95.14%   0.075

Findings:
1. Cacheable, but 20x weaker locality than cWD's stream (0.29 vs 0.0141
   misses/node at 18 bits). The aggregate ~2000x reuse is spread thin.
2. NO KNEE: ~5pp per 4x of size to 128 MB. Sizing is a budget call; 20-22 bits
   is the sensible range (64-256 MB total at W=8, noise next to 32.8 GB of
   tables).
3. The 8.9 GB page set in one slice confirms E4: the k8 working set can never
   be resident (~14 GB of RAM left after cWD + OS), so memo misses keep paying
   refault-class costs. No residency tuning escapes this.

Estimated lever value: ~20-30 ns/node (~a quarter of the k8 layer) at 87-92%
hit — enough to flip the 0.05% tie at 148, far short of the cWD cache's win.
Not built yet; the A/B cycle is ~40 min (memo in k8_child, regate 269,180,917 /
8,539,130,554, race 146 W=8).

Extended memo sizing: miss decomposition (2026-07-30, same 1.8B-node slice)
---------------------------------------------------------------------------
  distinct idx (HLL +/-0.8%): 58,788,203  -> mean reuse 47.2x, compulsory
  floor 2.117%. The working set is ~470 MB at 8 B/slot — reuse is HIGH but the
  live key set dwarfs any practical cache. (Prediction check: the "locally
  novel stream / single-digit reuse" hypothesis was WRONG; recorded so the
  reasoning isn't reused.)

  Associativity at equal capacity (LRU-in-set):
    2 MB:  dm 81.29 / 2-way 83.24 / 4-way 84.04
    32 MB: dm 91.87 / 2-way 93.04 / 4-way 93.50
  -> conflicts are +1.2-2.8pp; the misses are CAPACITY. Only size helps, and
  the flat ~5pp/4x slope is a skewed reuse tail: 1M slots (1.8% of the working
  set) already catch 87%.

  Per-table dm@20: a 90.09%, b 96.19%, c 86.51% — group c both recurs least
  and touches the most pages (3.78 GB): the workload scrambles c's tiles most.

  Warm-up: window hit rates flat from the first 512M probes
  (87.4/88.0/86.3/87.3/87.6) — sustained steady state, no cold-start artifact.

Design consequences:
  1. Key the memo by PACKED POSITIONS, not idx: bijective with idx (perfect
     ranking) so these hit rates transfer, and a hit then skips the rank AND
     the table touch — roughly doubling the lever (~40-50 ns/node of the 82 ns
     layer). That is enough to turn the measured 148 tie into a clear win.
  2. Direct-mapped, 22 bits (32 MB/worker), no associativity (capacity-bound).
  3. Caveat: sequential-stream rates are OPTIMISTIC for W=8 private caches —
     cross-subtree transposition reuse fragments across workers; expect high
     80s, not 92, per worker.

Positions-keyed memo cache — REJECTED at every size (2026-07-30)
----------------------------------------------------------------
Built as sized: direct-mapped (packed 8 positions + blank cell + table) -> h,
per arena, hits skipping BOTH rank and diff_lookup. Node-identical at every
size (269,180,917 / 8,539,130,554). Real hit rate 89.451% at 22 bits — only
2.4pp under the idx-keyed sim, so the blank-fineness cost was minor and the
sim's cacheability verdict was correct. And yet, W=8 exhaust-146:

  none (v2)   133.65 s          <- stands
  2 MB        138.44 s  +3.6%
  8 MB        148.69 s  +11.3%
  32 MB       151.4-155.7 s  +13-16%

MECHANISM (not a parameter): a memo must be cheaper to consult than what it
memoizes. The consult's average probe is ~25 ns — rank is the tight cache-hot
loop §8i measured (no cycle win from shaving), diff_lookup's HOT page subset
stays resident (1.8% of recursive samples) — while a random 8-B read into even
a 2 MB array costs comparable-or-more, and at 32 MB adds its own TLB misses
(2048 pages/worker vs ~3000-entry L2 TLB) plus 256 MB of RAM pressure evicting
table pages. The expensive table touches are the non-resident TAIL, and a
front-cache cannot pay only on the tail — it pays on every probe.

The reusable law, third form: sample share != reclaimable time (§8i);
LRU-model hits != direct-mapped hits (probe-cache-stats note); and now
HIT RATE != SAVINGS. The sizing measurement answered "would it hit" (yes,
89%) — only the A/B could answer "does a hit save more than the probe costs"
(no). Reverted to v2's direct rank+diff path; the k8-probe-locality
instrumentation and this record are kept.

*** MEASUREMENT CONTAMINATION FLAG (2026-07-30 afternoon) ***
-------------------------------------------------------------
The memo-cache A/B, its size sweep, the madvise(MADV_RANDOM) A/B, and the
revert-confirmation run (16:09-16:58 UTC) ran on a CONTENDED box: rust-analyzer
was re-running clippy-driver at ~98% CPU after each code edit (caught by ps
during the diagnosis), plus interactive foreground load — unlike the overnight
window, which was idle. Symptom that exposed it: 178 -> 200 -> 211 s monotone
degradation ACROSS a code revert, then a cWD-only canary at 108.7 s vs its
clean 91.5-92.2 s baseline (+18%) despite touching no k8 state.

Status of afternoon conclusions:
- Memo cache reject: size ordering (32 MB > 8 MB > 2 MB cost, non-monotone in
  run order) tracks the parameter, and the mechanism argument stands; but the
  "+13%" magnitude is NOT defensible. Needs one clean interleaved confirmation
  before being cited.
- madvise(MADV_RANDOM): UNRESOLVED, not rejected — most contaminated runs of
  the afternoon. Re-A/B cleanly. (The spatial-locality mechanism argued from
  the cohort-contiguous rank layout remains plausible in both directions.)
- ALL overnight results (148 race, cut ratios, v2 gates, 133.65 s, E4) were on
  an idle box and stand.

Lesson, again, sharper: "back-to-back, same session" is not sufficient — the
box must also be IDLE, and a canary run of the UNTOUCHED engine belongs in
every A/B batch, because it distinguishes environment drift from the change
under test in one run. The 108.7 s canary did in 2 minutes what three
mutually-confounded k8 runs could not.

Queued for the next idle window: interleaved v2-baseline / memo-18 / madvise
batch, with a cWD-only canary bracketing it.

Readahead-value measurement — madvise lever CLOSED without a clean A/B (2026-07-30)
-----------------------------------------------------------------------------------
Count-based (load-immune, valid on the contended box): on the page-event stream
(consecutive same-page probes collapsed — only 1.3% collapse, i.e. 98.7% of
probes change page), classify page faults at several eviction horizons and ask
whether a recent prior fault lies within a readahead window (±4/±8 pages, same
table), with a distance histogram.

  horizon      faults      cov+-4   cov+-8   D=1 | 2-4 | 5-8 | 9-64 | far
    50000   280,610,635   16.18%   23.83%   7.9 | 8.2 | 7.6 | 31.9 | 44.3
   200000   143,900,484   14.30%   21.25%   6.9 | 7.4 | 6.9 | 30.7 | 48.0
  1000000    57,900,411   12.50%   18.76%   5.9 | 6.6 | 6.3 | 29.2 | 52.1
  first-touch    542,956    8.67%   13.55%   4.1 | 4.6 | 4.9 | 25.1 | 61.4

Findings:
1. MADV_RANDOM's premise ("readahead is pure pollution") is WRONG: an 8-page
   cluster would absorb ~21% of faults, and the largest locality bucket is
   D=9-64 pages (~31%) — diffuse neighborhood reuse a ramping readahead can
   partially capture. Best case for the hint is gone; verdict
   neutral-to-slightly-harmful; the lever is CLOSED and does not earn an
   idle-box A/B slot. (Weak independent support for the direction of the
   contaminated afternoon regression.)
2. The trade readahead actually makes here: ~21% fewer fault stalls at ~equal
   SSD cost per fill, against ~6x page-cache churn (8 pages/fault vs 1) on a
   RAM-bound box. The counts can bracket but not settle that trade — kernel
   policy (cluster size, ramping) is unobservable — which is exactly why the
   verdict is "no clear upside" rather than a number.
3. CORRECTION to the mechanism story recorded with the madvise revert: 98.7%
   of probes change page, so there is almost NO adjacent-probe page locality
   from the cohort layout. The 87% memo-sim hit rates are TEMPORAL reuse at
   long distance; the spatial component lives at D<=64 pages across faults,
   not between consecutive probes.

Method note: this is the measurement that should have PRECEDED the madvise
edit, exactly as the sizing sim preceded the memo build — it costs one
budgeted run, is valid under load, and would have prevented a wasted A/B and
the contamination mess. Ahead-of-time pattern measurement now demonstrated on
both cache-shaped levers.

Slot sharing (v3) — WASH, reverted; the arena-store law predicted it (2026-07-30)
---------------------------------------------------------------------------------
Built the row_at/col_at-style sharing for the six k8 group-views: per-depth
GroupSlot (51 B) arrays + K8Frame{at, incremental view-sums}; a consult copies
only the two cost-1 group-views (306 B bulk copy -> 2x51 B + 10 B frame).
GATED EXACT on both counts, twice (269,180,917 / 8,539,130,554).

Canary-bracketed interleaved A/B (v2/v3/v2/v3), W=8, exhaust-146. Canaries
106.0/108.3 s vs the clean 91.5-92.2 s baseline: a STEADY +15-18% tax (a
videoconference), stable to 2% — so paired deltas are suggestive, not bankable:

  pair A: v2 182.96  v3 181.31   (-0.9%)
  pair B: v2 181.28  v3 183.83   (+1.4%)

A wash. And the recorded arena-store measurement from the flat-engine session
(~0.04%/byte; "10.5% share = 0.4% recoverable") already predicted it: the 306 B
copy costs ~1-2 ns, not the 10-20 the lever was scoped against. v3 swapped
nearly-free bulk stores for index bookkeeping plus SCATTERED reads (the shared
slot lives at an ancestor depth, off the contiguous parent line). Reverted to
v2 — simpler, gated, and the same speed. Clean-window re-measure declined: the
prior explains the wash, and +-2% would not justify six index invariants.

Lesson: scope levers against RECORDED costs, not estimates — the store law was
in this log all along.

k8 lever ledger after this: memo cache DEAD (mechanism), madvise DEAD (counts),
slot sharing WASH (store law). Open: k7-as-replacement shadow count; W=12.
The engine for the R >= 152 run remains gated v2.

PMU decomposition of the k8 layer (sudo, sequential, budgeted 146 window, 2026-07-30)
-------------------------------------------------------------------------------------
Thread-scoped counters (CORE_ACTIVE_CYCLE / INST_ALL / L1D_CACHE_MISS_LD_NONSPEC /
L2_TLB_MISS_DATA), --max-nodes 1.5B, idle box. Partial-146 windows:

  per node          cWD-only    2-tier     delta (the layer)
  cycles            127.41      341.67     214.3
  instructions      455.9       1195.9     740
  L1D miss          1.433       5.332      3.90
  L2 TLB miss       0.023       0.559      0.536  (24x the whole cWD engine)
  IPC               3.58        3.50       ~unchanged

Validity: cWD 127.4 cyc / 3.4 GHz = 37.5 ns vs 38.0 measured wall (agrees; cWD
spends ~nothing off-core). Layer per consult: 279 cycles ~= 962 instructions at
the same IPC (self-consistent).

THE MEASURED SPLIT of the ~110 ns consult:
  ~82 ns  on-core, THROUGHPUT-BOUND: 962 instructions/consult — more than 2x
          cWD's entire node (456) — at undiminished IPC 3.5. The added L1D/TLB
          misses are absorbed by the OoO core.
  ~28 ns  off-core, BLOCKED: active cycles cover only ~100 ns/node of the
          2-tier's 122.6 ns wall; the ~22 ns/node gap is hard-page-fault
          deschedule time (cWD-only shows ~0.5). The cold tail, measured
          directly: ~26% of the layer.

Consequences:
1. Guess scorecard (vs the retracted estimates): ranks ~45 plausible (inside
   the 962-inst budget; duplicate-rank ablation to pin), probes ~25 ~= the
   28 ns blocked term, carriage ~15 WRONG (near-free, store law).
2. k7 lever priced before its shadow count: fully-resident tables delete the
   ~28 ns blocked term + most of the 24x TLB multiplier => ~25-30% of the
   layer, IF k7's cut ratio holds. The cut stays the only decisive unknown.
3. Any further shave of the ON-CORE 82 ns must remove instructions from a
   3.5-IPC stream — the §8i law's territory, where five levers have now died.

Duplicate-rank ablation — the rank term measured (2026-07-30, CLEAN window)
---------------------------------------------------------------------------
Canary-bracketed (94.00 / 91.44 s vs the 91.5-92.2 clean band — first valid
timing window since overnight; the v2 arms also reproduce the overnight
baseline: 133.90/131.09 vs 133.65 s, which retroactively firms the memo-reject
magnitudes). Ablation binary = v2 + one extra black_box'd rank per view per
consult; node-identical by construction and verified (both counts exact).

  v2 mean 132.5 s | +2-ranks mean 155.8 s | delta 23.3 s at W=8 over 8.539B
  => 21.8 ns/node CPU => 28.4 ns/consult for TWO hot ranks => ~14 ns/rank

FLOOR caveat: the duplicate rank runs on just-ranked hot data (shape_info line
resident); the real first rank pays that fetch. Real pair in [28, ~40] ns.

The 82 ns on-core consult therefore splits: ranks 28-40, remainder 42-54
(slides, 306 B copy [~free, store law], diff bit-decode, TAU/SIGMA/group glue,
call overhead, absorbed L1D/TLB latency). Consequence: dropping the reflected
view removes ~14 ns of rank + one probe + one slide, NOT half the layer — its
case now rests mainly on halving the blocked/probe side and the node arithmetic.

Partial-PDB ("winner set") feasibility — DEAD in one count-based run (2026-07-30)
---------------------------------------------------------------------------------
Idea: keep only configurations whose table value beats a cheap admissible
fallback (group Manhattan), store them sparsely (absolute values, hashed
positions — no rank, no diff-chain, no 32.8 GB residency). Instrumented the
live engine (it already carries true h per group-view; MD computed from the
same positions): per consult, all six (h, MD) pairs + the prune bound, with the
prune test replayed at every surplus threshold s.

  1.15 B consults, 14.95% actual prunes. Surplus is parity-locked (even only).

  s   prunes kept   changed-probe hit rate
  2     100.00%       62.33%     <- only cut-preserving threshold; "winner set"
                                    ~ 60% of the space = hundreds of GB. Dead.
  4      25.45%       27.17%     <- first set that shrinks; cut collapses. Dead.
  6       2.48%        3.51%
  MD-only  0.00%                 <- the fallback floor is ZERO.

  Surplus histogram (all six views): 42% at 0, 32% at 2, 24% at 4, 1.7% at 6.

Verdicts:
1. k8's edge over Manhattan is BROAD AND SHALLOW: +2 almost everywhere, prunes
   assembled from six small surpluses summing. No elite exists to keep.
2. MD-only retains zero prunes, forced in hindsight: a prune must beat cWD and
   cWD >= WD >= sum of group MDs by construction.
3. The structural root: "beats cWD" does not factor across the partition — cWD
   is a whole-board value, so no per-group keep/drop rule can align with the
   prune condition. Same diffuseness the cache sims measured (47x thin reuse,
   no knee), now at the value level.

Global cold_lookup sampling harness skipped — no viable s to size.

k8 lever ledger: memo DEAD, madvise DEAD, slot sharing WASH, partial-PDB DEAD.
Open: reflection drop (full-exhaust node ratios needed at 146/148), k7 shadow,
W=12. Rank term now measured (~14 ns hot, ablation above).

Prune-config harvest — workload-defined sparse sets also DEAD (2026-07-30)
--------------------------------------------------------------------------
Question: do the configs participating in successful prunes form a small,
stable set we could keep sparsely (absolute values, hashed positions — no
rank/1-bit chain)? Two-phase single-process measurement: phase A harvests the
full 144 iteration's load-bearing configs (surplus >= 2 at a prune) into a
Bloom set; phase B (146 slice) harvests its first 50M prune events into a
second set and replays all later events against both (event "reproduced" iff
serving map-hits h and misses MD still clears the bound).

  phase A (144): 22.4M prune events, distinct load-bearing configs ~8.8M (10.3x reuse)
  phase B: 149.6M prune events in 1.23B nodes; half-set distinct ~18.3M;
           all-B distinct ~41.4M AND STILL CLIMBING (1/7 of the threshold)
  held-out coverage:           events    configs
    by 144 complete set        39.86%    65.83%
    by B-first-half set        35.37%
  load-bearing views/event [1..6]: 0.07M / 9.4M / 66.7M / 51.2M / 35.9M / 8.8M
  winning view: normal 98.5M, reflected-only 46.1M (26.8%!), both 27.4M
  prune depth: peak plies 32-44

Verdicts:
1. THE CEILING IS 35%: a set harvested minutes earlier in the SAME threshold
   misses 2/3 of later prunes. Pruning configs are SUBTREE-LOCAL — the DFS
   suffix visits fresh ones (which is also why the complete-144 set beats the
   B-prefix set despite the threshold gap).
2. Prunes are CONJUNCTIONS: median 4 of 6 group-views load-bearing, so event
   coverage (~40%) collapses far below config coverage (~66%).
3. Bonus datum for the reflection lever: 26.8% of prunes come ONLY from the
   reflected view — consistent with the recorded +17.3%-nodes cost of dropping it.

With the surplus experiment this closes the sparse-k8 family from both ends:
no value-defined elite exists (surplus broad+shallow), no workload-defined
stable core exists (harvest subtree-local+conjunctive). The tier is
irreducibly dense, diffuse, and conjunctive. Open levers unchanged:
reflection-drop (needs full-exhaust node ratios), k7 shadow, W=12.

Pruner tile-structure — the edge is NOT conflict-shaped; family fully closed (2026-07-30)
-----------------------------------------------------------------------------------------
656M load-bearing pruner configs vs a 1/64 background sample (91.8M configs).

  surplus x lc-pairs cross-tab (% of load-bearing configs):
              lc=0    lc=1   lc=2   lc=3+
  surplus 2   44.92   6.61   0.09   0.00
  surplus 4   36.05   6.13   0.57   0.06
  surplus 6+   3.37   1.90   0.23   0.06

84% of surplus-carrying configs have ZERO linear conflicts — including 36% of
mass at +4-with-no-conflict. The classic LC mechanism explains <=15% of k8's
edge; no MD+2LC-style per-group formula can substitute for the table. The
surplus is irreducibly zero-aware interaction information.

Commonalities found (real, not exploitable):
  - group b carries the surplus: a/b/c = 18.1/52.3/29.5 vs uniform 33/33/33
    background (b was also the most cacheable group in the memo sims);
  - the blank sits at corners, above all its goal corner: cell 24 holds 10.4%
    of pruner configs vs 2.4% background (pruner heat edge-skewed, background
    center-heavy) — i.e. the found structure IS the zero-aware region
    constraint, the very thing the table adds over blank-agnostic PDBs;
  - pruners slightly more ordered than background (displaced-of-8: 77.9% vs
    84.8% fully displaced).

This completes the k8 characterization: dense, diffuse, conjunctive, and
formula-resistant. Every sparse/cheap substitute is now measured dead:
memo, madvise, slot sharing, surplus-defined sets, harvested sets, LC-family
formulas. What the tier costs (110 ns/consult: 82 on-core throughput-bound +
28 blocked) is what its information costs. Open levers unchanged: reflection
drop (full-exhaust ratios), k7 shadow, W=12.

Blank-centric partition probe — k=6, full-space, exact (2026-07-31)
-------------------------------------------------------------------
Question (from the tile-structure findings): does partition geometry relative
to the blank's goal corner concentrate zero-aware surplus? Probe: build k=6
ZPDBs in memory (~29 s each, 181,008,000 entries) and measure the EXACT
full-space surplus (dist - group-MD) distribution per pattern. Test:
`cargo test --release probe_blank_centric -- --ignored --nocapture`
(kept in zbuild.rs with the canonical 13-pattern list).

  pattern                          >=2      >=4     >=6    mean
  RING-1/CORNER (2x3 at home)     89.79%   44.33%  8.05%  2.853
  ROWBAND-D (bottom band)         84.14%   27.87%  3.64%  2.317
  QUAD-D (corner quad)            83.72%   27.83%  2.86%  2.290
  QUAD-A/B/C (far compact blocks) 47.01%    9.96%  1.03%  1.161  (identical x3
                                                          — sigma-congruent)
  BAND-A/B/C                      31-40%   3-6%    <1%    0.68-0.90
  RING-2/3 (thin shells)          20-27%   1-2%    ~0%    0.43-0.57
  SCATTER (diagonal control)      16.53%    0.68%  0.01%  0.344

  Partition totals (sum of group means — the additive currency):
    HYBRID  (CORNER + QUAD-A + QUAD-C + serpent{4,5,9,10,13,18})  5.976  BEST
    QUADRANTS (existing pdb24_a..d)                               5.773
    RINGS   (pure blank-centric)                                  4.886
    ROW-BANDS (existing rb_a..d)                                  4.776

Findings:
1. Blank proximity concentrates surplus in a GROUP: 2.5x mean, 4.4x tail vs
   the congruent far block (CORNER vs FAR isolates geometry from compactness;
   compactness itself is worth 3.4x over scatter).
2. A PURE blank-centric partition loses: thin-shell complements collapse to
   near-scatter. Every group's compactness dominates one group's corner.
3. The HYBRID (compact blocks everywhere, tightest block on the blank's home)
   beats the best existing partition by +3.5% total surplus.

Scope: k=6, global-space, surplus vs group-MD — not workload-weighted and not
prune power vs cWD. Reasons to expect the workload effect to be LARGER: the
proof contour parks the blank at its goal corner 4x over background (measured,
tile-structure run), weighting exactly the group the hybrid strengthens.

k8 implication, unbuilt: the analogous move is one tight 8-tile corner block
{13,14,15,18,19,20,23,24} (the 3x3-minus-blank at bottom-right) replacing the
current right-edge snake as group b, complements re-compacted. Costs ~26 h of
table builds to test for real; the cheap intermediate is a k=7-scale probe
(~3.4 GB in memory) or workload-shadowing the k6 hybrid in the engine.

Blank-aware demand candidates — tested on the yielddiag protocol (2026-07-31)
-----------------------------------------------------------------------------
Harness: slack_anatomy `blankdiag` (new mode, branch `blank-aware-demand`,
worktree of pre-deletion main), 3000 exact-solved len-55 boards + R 156-path
bind analysis — §8p/§8q's exact protocol.

  V1 dem>=1 & blank-out (+1):  unsound 24/3000 (0.80%) — dead (8x R2's rate).
  V2 blank-out & e>f (+e-f):   fires NEVER — vacuous by identity (blank out
                               => every missing resident's cell is foreign,
                               f >= e always). Design error, recorded.
  V3 pocket (blank in home line, residents missing, +1):
      binds 64/157 R-corridor states, total +142 (~2.2 each) — NOT
      LIS-subsumed, the first rule to genuinely bind since R1;
      unsound 4/3000 (0.133%, overshoot 2.0) — DEAD as stated (GB died
      at 0.07%).
      Counterexample mechanism (4/4 identical, all row 4): the blank's own
      goal is IN the home line, so endgame entries interleave with foreign
      ejections at zero waste; the rule charges the finale. Obstructive vs
      scheduled occupancy is choreography-global — line-local predicates
      cannot see it (§8p's free-slot law, blank-side form).

Net: the pocket INFORMATION is confirmed real and binding on the proof
corridor; every static line-local charge for it tested so far is unsound.
The information's sound carriers remain the tables (exhaustive BFS) — or an
O(1)-cold encoding that would make table consults gateable (see the
capped-surplus proposal below, unmeasured).

V4: last-moves/corner-tiles (Korf-Taylor) vs cWD — FIRST SOUND+BINDING RULE (2026-07-31)
----------------------------------------------------------------------------------------
Motivated by the prune autopsy (the +2 is corner-rotation cost; tile 24's
23->24 overshoot the dominant motif). Classic K&T charges tested on the
3000-board protocol:

  V4a last-moves:        binds 57/157 corridor (+114); unsound 3/3000 — leak
                         diagnosed: charge-sharing with the LIS escape demand
                         (same excursion billed twice).
  V4b corner-tiles:      sound 0/3000; zero corridor bind (R1's fate).
  V4d last-moves + guard (fire only when dem_row[3]==0 and dem_col[3]==0):
                         SOUND 0/3000 AND full 57-state/+114 corridor bind
                         retained — the guard excises exactly the three
                         counterexamples, none of the corridor.

Status: empirically sound at 0.1% resolution (same standard that certified
R1); not a proof. O(1)/node: positions of tiles 20/24 (maintainable
incrementally) + dem[3] per axis (already in the axis slots). Fires on 14% of
random boards, 36% of R's corridor states, +2 each.

Next: (a) 30K-board soundness run to tighten the bound; (b) integrate into
the flat engine as h = cWD + charge behind a flag (validity gate: optimal
lengths vs the frozen oracle's Solved cases — node identity does not apply to
a changed heuristic); (c) node-count + wall A/B at 144/146, including the
2-tier interaction (V4d overlaps k8's +2 territory, so it shifts iso-time for
the tier too).

Leak #2 diagnosed; V5 defined: Last-Move Escape-Constrained Walking Distance (2026-07-31)
-----------------------------------------------------------------------------------------
Leak #2 (V4d's 1/30,000): dumped counterexample has dr = dc = ALL ZERO and
d = h0 exactly — the rotation's two moves ride transfers plain WD's own
blank-routing accounting already billed. WD-INTERNAL ABSORPTION: the sharing
lives inside the table value, invisible to any board-feature predicate. This
closes the guard-based (V4) family with a mechanism: the 0.1% -> 0.003% guard
trajectory was converging to this floor, not to zero.

V5 — named "Last-Move Escape-Constrained Walking Distance" (code: cwd-lm):
    h = min( cWD(dem, dem_col[3]+1),  cWD(dem_row[3]+1, dem) )
with a branch degenerating to h0 when its tile (24 / 20) already sits in its
goal or free-ride line. The last-move disjunction (every solution sends tile
24 or tile 20 through cell 24, forcing a goal-line-3 escape) enters as a
PRICED DEMAND through the constrained A* — the same machinery as cWD's LIS
demands — so a pre-billed excursion returns marginal 0 and double-billing is
impossible by construction. The leak-#2 board is V5's thesis case: tile 24 in
col 3 degenerates that branch; tile 20's branch prices at 0 (absorbed);
charge 0; sound.

Certification pipeline queued: 3K fast check, then 30K on the seed base that
caught both prior leaks. Pass = 0 unsound + corridor bind retained (V4 bound
57/157 states, +114).

V5 certification: SOUND AND EMPTY — the family closes both ways (2026-07-31)
----------------------------------------------------------------------------
V5 (Last-Move Escape-Constrained Walking Distance): unsound 0/30,000 — and
fires on 0/157 corridor states, 1% of random boards (V4d: 57 states, 14%).
The constrained A* prices the extra escape at ZERO nearly everywhere: the
token relaxation's routing freedom absorbs one added line-3 escape at no
cost. Same fact as leak #2 from the other side — WD-internal absorption is
not a rare edge, it is the norm at the abstraction level; reality just
usually cannot exploit it (V4's +2 held on all but 1/30,000 fired boards).

Verdict: the last-move +2 is (a) true on ~all fired boards, (b) unprovable at
the WD abstraction — assertable only unsoundly, priceable only at ~0. Its
proof requires finer-than-WD structure: exactly what the 8-tile tables carry.
Rule-based capture of the PDB's +2 is closed with mechanisms in both
directions (additive: absorption leak; demanded: absorption pricing).

Engineering track unchanged: reflection-drop A/B, partition qualifiers, k7
shadow, and the R >= 152 run itself.

cwd_lm table built and verified (2026-07-31)
--------------------------------------------
Enlarged sample first: 507 workload prune boards — V6 joint certifies 65.7%,
table-design (separable) form 43.2% +/- 4.3 of k8's +2s. Above threshold; built.

src/puzzle24/search/cwd_lm.rs: backward BFS over (WD key x tile-line x
crossed-flag) from the goal (tile home, crossing made). Reached exactly
65,650,495 keys = the full known WD state space (structural validation);
118 s build; D(goal, line 3) = 2 sanity passed; data/cwd_lm.bin = 853 MB
(one table serves both axes).

Cross-validation: worktree `lmcheck` compares table values against the
independent refined_axis A* on the 507 boards — 1013 branch values, 0
mismatches.

Next: flat-engine integration (track row(20)/col(24) incrementally; h =
max(cWD, min(branch20, branch24)) with line-4 degeneracies), gate on frozen
oracle optimal lengths, then the node/wall A/B at 144/146 vs 422,379,806 /
91.5 s.

cwd-lm integrated into the flat engine; first A/B (2026-07-31)
--------------------------------------------------------------
Integration mirrors the k8 tier: lazy consult at cWD-survivors only;
per-depth (row20, col24) tracked with the entered-node invariant (debug-assert
against a board scan, exercised on a 2M-node debug run); branch20 = D(row key,
row20) + full col term, branch24 mirrored; h_eff = max(h_cwd, min(b20, b24));
line-4 free-ride degeneracies. `solve24 --lm` (refuses --zpdb8 combo — dispatch
arms deliberately unwired until wanted).

Canary-bracketed W=8 A/B (canaries 93.39 / 92.79 s — valid window):

              144 nodes      144 wall     146 nodes        146 wall
  base        422,379,806    1.90-1.95s   17,767,093,830   93.8-94.0s
  +LM         267,641,675    1.72-1.75s   11,198,907,473   92.3-93.3s
  cut/cost    1.578x         ~10% faster  1.586x / 1.567x  ~1% faster (iso)

Node counts deterministic across repeats and drivers (sequential 144 matches
W=8 exactly). Notable: LM alone BEATS the whole k8 tier at 144 (267.6M vs
269.2M) — the last-move branches are whole-board WD information, not a subset
of the 8-tile groups' view. LM's cut is FLAT with depth (1.578 -> 1.586),
unlike k8's growing cut — one fixed obligation vs compounding conjunctions.

146 verdict: iso-time (cut 1.586x vs cost 1.567x; 42.3 -> 66.3 ns/node). The
cost is dominated by two raw probes into the separate 1.2 GB LM map per
consult — SAME axis keys as the engine's cWD probes, so cWD-class locality.
Next: a direct-mapped LM front cache (ProbeCache design); expected cost ratio
~1.1-1.2x -> ~1.3-1.4x wall win at every threshold, no depth extrapolation.

LM front cache: the cut converts to wall time (2026-07-31)
----------------------------------------------------------
Direct-mapped key -> [4 branch values], one map probe fills all lines; 18 bits
= 4 MB/worker, FLAT_LM_CACHE_BITS overridable; in-situ counters from birth.
Sizing taken from measured priors (LM probes = the axis-key population the
cWD ProbeCache serves at 98.9-99.7%), not a fresh sim — and the in-situ rate
confirmed it: 99.588% at the default size.

Canary-bracketed W=8 A/B (canaries 91.72/90.90 s; node counts byte-identical
to the uncached runs — transparent cache verified):

           144                 146
  base     1.89-1.94 s         90.90-93.86 s
  +LM      1.30 / 1.31 s       69.28 / 69.67 s
  speedup  1.47x               1.32x

Per-node 41.3 -> 49.5 ns (cost 1.20x) against the flat 1.586x cut. The
cWD-only configuration's improvement is in hand: depth-independent by
construction (the last-move obligation binds identically at every threshold),
853 MB + 4 MB resident, ~24 s off every exhaust-146.

Open, unbuilt: joint-form tables (66% vs 43% certification), corner-tile
analogues for the other corners (each costs a x5 state dimension), --lm with
--zpdb8 (cascade wiring), and a 148 run to confirm the flat-cut claim one
rung deeper.

Joint-vs-single-line-max cWD gap — measured, RECORDED, not pursued (2026-07-31)
-------------------------------------------------------------------------------
Production cWD's surcharge is the single-line-MAX of the per-line marginals
(the cwd_single fast path); the reference vector-constrained A* prices the
demand vector jointly. Measured (harness `jointgap`, 30K random len-55 +
507 workload prune boards, no exact solving needed):

  population   multi-demand axes   gap|multi   boards w/ gap   gap sizes
  random             6.49%           73.0%        9.37%        91% +2, 9% +4
  workload           5.23%           60.4%        6.31%        all +2

Pre-registered predictions, graded: P1 (multi-demand 8-15%) close, low side.
P2 (gap|multi 10-30%) BADLY WRONG at 73% — the V5-absorption reasoning does
not transfer: two demanded escapes are separate mandatory excursions of
different tiles; absorption needs slack for both, max() bills only one.
P3 wrong on random (9.4% vs 1-4%), in-band on workload; the contour-enrichment
hypothesis REVERSED (random > workload — the contour is demand-sparser).
Gap-size prediction right.

Meaning: production cWD is provably loose by +2 on ~6.3% of real workload
boards. Rescue architectures, sorted by this data:
  - runtime joint-A* fallback: DEAD (~10 ms/eval vs 50 ns nodes);
  - full pairwise curves: ~21 GB, not warranted;
  - pairwise-(1,1) overlay: joint marginal per state per line-pair at demand
    (1,1) — ~10 B/state ~= 656 MB, ten cwd_single-style sweeps, same
    key/cache path. Expected ~1.1x further node cut, stacking with LM's
    1.586x (and lifting LM's own branch values, which use the fast-path
    surcharge on the un-refined axis).

DELIBERATELY NOT PURSUED for now (user decision 2026-07-31). If revived, the
first step is the (1,1)-dominance check: what fraction of gap-bearing states
have both demands at level 1 (one-line extension of the jointgap mode).

================================================================================
LM2: last-TWO-moves certification probe (harness lm2test / lm2boards)
================================================================================

Extension of LM one step deeper into the endgame. Every optimal solution's
final two moves pin the blank's suffix to one of four cell paths
(14>19>24, 18>19>24, 18>23>24, 22>23>24), giving a 4-branch disjunction of
per-axis obligations:

  A: row pair  -- tile 20 (type-3: cross 3>4, end 3) + tile 15 (type-2:
                  cross 2>3, end 2) + full col term
  B: row(20) + col(19)   -- tile 19 is type-3 in BOTH axes (goal cell 18),
  C: row(19) + col(24)      so B/C reuse the existing single-tracked form
  D: col pair  -- tile 24 + tile 23 + full row term

lm2 = max(cWD, min(A,B,C,D)) >= LM by construction (each branch dominates
an LM branch). Per-branch degeneracies (tracked tile already at/past its
crossing line) drop that tile's constraint, mirroring the p20-row-4 rule.
Length-<=1 guard: goal and the two pre-goal states take h_cwd (no
penultimate-move obligation exists there).

Implementation: refined_axis2 (slack_anatomy.rs, worktree) -- refined_axis
generalised to track up to two tiles (type-3 + type-2) in one axis;
refined_axis2(Some(l), None) == refined_axis(l) asserted on sample boards.

Soundness (lm2test, 3000 random len-55 boards, exact solve, both forms):
  0/3000 unsound (joint and table forms); 0 budget fallbacks.
  Binds on randoms: joint 20.43% (+2.04 mean), table 10.50% (+2.05 mean).

Certification (lm2boards, the 507 k8-prune-event autopsy boards; baselines
reproduce prior v6boards numbers exactly):

  form    LM cert      LM2 cert     beyond-LM boards   hist(lm2-h0)
  joint   333 (65.7%)  378 (74.6%)       45            129x0, 368x+2, 10x+4
  table   219 (43.2%)  295 (58.2%)       76            212x0, 285x+2, 10x+4

Table-realizable certification of k8's prune events rises 43.2% -> 58.2%
(+15.0 pts, a 1.35x rate increase); 10 boards (2.0%) now reach +4 --
DEEPER than k8's own +2 on those events. The buildable shape:

  - branches B/C: served by the EXISTING cwd_lm.bin -- the table is
    identity-generic over type-3 tokens, so tile 19's line indexes the same
    5-value record. Zero new build; engine change = 4 extra cache probes.
  - branches A/D: one new pair table D(key, line3, line2) with both
    crossing flags folded at build -- 25 values/key, ~2.2 GB at the CWLM
    record shape, same backward-BFS builder with a 100x state multiplier
    (~656M x ~10 effective; build est. tens of minutes).

NOTE a B/C-only integration (skipping the pair table) is NOT an option:
the disjunction is a min over exhaustive endgame scenarios, so branches
A/D degrade to single-tracked b20/b24 -- which undercut B/C (B = r20+c19
>= r20+cc = b20), collapsing the whole bound to LM identically. The pair
table is the price of any gain.

Not yet decided; recorded for ranking against the parked pairwise-(1,1)
cWD overlay (~656 MB, ~1.1x est.). Untried within this family:
last-THREE-moves (16 branches, triple tracking, x5 state again).

--------------------------------------------------------------------------------
LM2 BUILT, VALIDATED, INTEGRATED, A/B'd (decision: build — user 2026-07-31)
--------------------------------------------------------------------------------

Build (cwd_lm.rs build_cwd_lm2, backward BFS over key x la x ca x lb x cb,
bitmap frontier over key indices, per-key edge work amortised across the 100
tracked codes): 65,650,495 keys, 2,346,712,861 reachable pair states (of
6.57B nominal), depth 72, 203 s single-thread, data/cwd_lm2.bin 2.17 GB
(CWL2, 33 B records). In-build checks: D2(goal,3,2) = 4; pair >= single
dominance on 1.79M placements.

Cross-validation (harness lm2check): table vs independent refined_axis2 A*
on the 507 workload boards, engine query shape (row key, l20, l15) /
(col key, l24, l23): 851 pair values, 0 mismatches, 0 defined/undefined skews.

Engine (--lm2, flat.rs lm2_child + Lm2Cache): four-branch min over the
endgame suffixes; B/C read the single table (tile 19 is type-3 in both
axes); pair probes only for A/D. Combined front cache (one tag -> 4 single
+ 25 pair values, 64 B slots, 2^17 default, FLAT_LM2_CACHE_BITS): 99.811%
in-situ hit at exhaust-144 (vs LM cache's 99.588%). --lm path untouched
(separate cache + position arrays). Degenerate branches fall back
pair -> single -> axis term, so a fully degenerate branch equals h_cwd.
Same goal-board caveat as LM (obligations presume >= 2 remaining moves;
exhaust regime never consults there; both pre-goal states safe by the
line-4 degeneracy). Debug lm2pos-drift assert clean over 2M nodes.

A/B (canary-bracketed, idle box, parallel, R, exhaust-144 + 146; canaries
91.28 / 90.98 / 90.78 s — clean):

            nodes@144     cut    nodes@146      cut    wall@146   vs base  vs LM
  base    422,379,806      1x  17,767,093,830    1x    91.0 s       1x      —
  --lm    267,641,675  1.578x  11,198,907,473  1.586x  70.6 s     1.29x     1x
  --lm2   216,158,191  1.954x   8,975,456,521  1.980x  64.6 s     1.41x   1.094x
  (k8     269,180,917  1.57x    8,539,130,554  2.08x   — 32.8 GB mmap)

lm2 wall@144 1.16 s vs base 1.90 s. Node cut grows with depth (1.954 ->
1.980), unlike LM's flat 1.586; at 146 the 2.2 GB pair table is within 5%
of the 33 GB k8 tier's node count. Sequential and parallel node counts
identical (216,158,191 @144).

Certification-to-cut calibration point #2: 43.2% -> 1.586x (LM),
58.2% -> 1.980x (LM2) — the superlinear mapping held.

The cWD-only configuration for deep thresholds is now --parallel --lm2.
Untried within the family: last-THREE-moves (16 branches, triple tracking,
x5 state again); joint-form tables; pairwise-(1,1) cWD overlay (stacks with
LM2's un-refined-axis terms).

Slack gate: measured DEAD. Consult-slack histogram (need - h_cwd at every
LM2 consult, stats build): exhaust-144: 151,492,026 consults, 100.000% at
slack 0 (forced: at bound = h0 consistency pins every survivor to f = bound).
exhaust-146: 6,779,472,719 consults, 95.41% slack 0 / 4.59% slack 2, 0 at
>= 4. Gateable (>= 7, the +6 splice cap) = 0.000% at both. Parity makes this
permanent: slack steps by 2 from 0, so >= 8 first exists at bound h0+8 = 152,
and that population is the f <= 144 contour of a 152 tree (~40x shrink per
+2 threshold) — negligible forever. Side-finding: lm2 cache hit 99.811% @144
degrades to 99.007% @146 (137.6M misses) — the 2^17 front cache is
undersized for deep thresholds; FLAT_LM2_CACHE_BITS sweep is a live lever.

Profile (sample, 30 s inside sequential exhaust-146, profiling build):
run_iteration 40.0%, build_child 28.7%, lm2_child 18.2%, child_lb 10.2%,
hashbrown get_inner (front-cache misses) 3.0%. The lm2 tier costs ~21% of
sequential time (consult + misses) for the 1.98x cut. Profile-driven
shortlist, in rank order:
  1. row-first early-out: branch A needs only the rkey probe; if
     A <= h_cwd then heff = h_cwd and the ckey probe + B/C/D compose can
     be skipped. Node-identical by construction. Fraction of consults
     eligible unknown — A/B directly.
  2. FLAT_LM2_CACHE_BITS 17 -> 18/19: hit 99.007% @146 (137.6M misses);
     LM's measured size curve suggests roughly halving misses per
     doubling. Attacks the 3% hashbrown share + 64B fill traffic.
  3. extract values inside the cache probe (return the 3 needed bytes,
     not the 29-byte slot copy) — stack-traffic trim, likely small.

Profile shortlist A/B'd (canary-bracketed x3, idle box; one batch retracted
for a stats-binary mixup — the saved "plain" reference was actually the
probe-cache-stats build, whose shared atomic counters ping-pong under 8
workers for a 4-5x slowdown (lm2_child 59% of samples vs 18% sequential);
lesson: never time the stats build in parallel, and verify binary identity
by strings before an A/B. One batch retracted for a mid-batch Microsoft
Defender vulnerability sweep (find_jar_packages.py + epsext, ~2 cores;
canary1 drifted 64.2 -> 68.1 s).

Clean results (146 wall, canaries 64.23/64.96/64.59 s):
  row-first early-out:  64.14 s vs 64.23 s baseline — NEUTRAL as measured
    (skipped col probe is a cheap predictable hit; consistent with the
    dead-ends law). REVERTED.
  FLAT_LM2_CACHE_BITS:  17: 64.2-65.0 | 18: 61.86 | 19: 61.82-60.97 |
    20: 60.88. 17->18 is the solid step (-2.4 s, 3.6%); 18/19/20 cluster
    within ~1 s (noise band ~0.7 s). Slot 64 B, so 17/18/19/20 = 8/16/32/64
    MiB per worker.
  Default set 17 -> 19 (32 MiB/worker, 256 MiB total at 8 workers): captures
  the full measured gain with margin for the deep-threshold working-set
  growth (hit rate fell 99.81% @144 -> 99.01% @146; expect worse at 148+;
  FLAT_LM2_CACHE_BITS=20 is a free knob for the 152-scale runs).

Net --lm2 @146: 60.9-61.9 s vs base 91.0 s = 1.47-1.49x wall (was 1.41x).

Probe-value trim + 32-byte slot repack (canary-bracketed, idle box):
  get3 (3-byte probe return vs 29-byte slot copy): the copy provably
  survives inlining (2 q-register pairs per probe in the disassembly —
  owned-tuple return from &mut self cannot alias the slot), but removing it
  is NEUTRAL (trim 60.86/61.59 vs canaries 61.01/61.68/61.43): predictable
  L1 stack traffic is free, per the dead-ends law. Kept as substrate.
  Slot repack 64 B -> 32 B: only 12 of 25 pair placements are queryable
  (la <= 3, lb <= 2; the rest are caller-sentinelled degeneracies), so
  tag 8 + single 4 + pair 12 = 24 real bytes, align(32), four slots per
  128-byte line (hw.cachelinesize = 128 on this box). Node-identical.
  Curve at 146 (32-byte slots): 19: 60.19 | 20: 60.43 | 21: 59.43/59.53 |
  22: 59.30; slot64@19 canaries 60.44/61.22/60.48/60.44. Knee at bits 21
  (64 MiB/worker, 512 MiB at 8 workers); 22 adds 0.2 s = noise.
  Default set 21; assert ceiling raised to 24 for the deeper-threshold runs.

Optimization pass net: --lm2 @146 64.6 s -> 59.4-59.5 s (-8%);
vs base 91.0 s = 1.53x wall (was 1.41x at ship). Sequence of levers:
cache 17->19 (+4-5%), repack-enabled 19->21 at 32 B slots (+1.5-2%),
early-out and copy-trim neutral, slack gate structurally dead.

Setup tier-3: mmap'd open-addressing artifact for the LM2 tier
(data/cwd_lm_mm.bin, 4 GiB: 2^27 x 32 B slots, key 8 + single 4 + pair 12,
linear probing at 0.489 load, longest chain 48, key 0 = empty sentinel —
the zero key decodes to an impossible token matrix). Built from the
canonical CWLM/CWL2 files with full 65,650,495-key round-trip verification;
--lm2 now mmaps this and parses nothing. Lazy demand paging initially
leaked +1.2 s @144 / +1.5 s @146 of faults into the search phase; fixed by
an eager pre-touch at load (one byte per 16 KiB page, sequential). Final
canary-bracketed A/B (canaries 59.37/59.70/59.50 @146):
  setup 43.5 s -> 31.2 s (-12.3 s; remainder is Cwd::new());
  search @146 58.92/58.88 s — ~0.5 s BETTER than canaries (one flat-file
  linear probe per front-cache miss vs two HashMap probes).
Next per user direction: --lm miss-fills from the same artifact (its
singles are already in the slot); then the same pattern for the cWD merged
table (CwdCell = 21 B fits the same 32 B slot geometry; hottest path, needs
a monomorphized backend swap + full bracket).

cWD merged table on the same artifact pattern (data/cwd_mm.bin, 4 GiB:
2^27 x 32 B, key 8 + curves 10 aligned at offset 8 + wd 1 + nbr_wd 10;
snapshots the merged table WITH nbr_wd filled, so mm_only() skips the WD
load, the overlay merge and the neighbour pass). MergedBacking enum
(map ref | mm ref) carried where &CwdMerged went — hit path untouched,
one predictable branch on front-cache misses only. Built + 65,650,495-cell
round-trip verified in 37 s. Node-identity holds on all three configs
(base 422,379,806 / lm 267,641,675 / lm2 216,158,191 @144).
Canary-bracketed A/B (--lm2, 146): search parity (canaries
63.73/60.25/59.78 vs mm 63.62/58.75 — batch ran warm, pairwise adjacent
comparison neutral-to-better); setup 27-33 s -> 0.9-5.5 s.

Tier-3 setup program complete: --lm2 launch-to-search went 43.8 s at the
start of the evening to ~1-6 s (pre-touch of 8 GiB of artifacts is the
whole cost; page-cache warmth decides where in that range). solve24 falls
back to the HashMap path with a build hint when an artifact is missing.

(1,1)-dominance check (harness dom11; the pre-registered gate for the
pairwise-(1,1) cWD overlay). On gap axes (joint > single-line-max), does
max(smax, best line-pair joint at demand (1,1)) recover the full joint?
Predictions graded: P1 (pure-(1,1) shapes recover by construction) held;
P2 (workload >= 80%) RIGHT at 93.8%; P3 (random gap-moves 85-90%) RIGHT
at 88.0%.

  random   n=30000: 2844 gap axes -> FULL 2488 (87.5%), PARTIAL 244,
           NONE 112; moves 5464/6208 (88.0%); shape 2379 pure-(1,1) / 465
  workload (507):   32 gap axes -> FULL 30 (93.8%), PARTIAL 0, NONE 2;
           moves 60/64 (93.8%); shape 28 pure-(1,1) / 4

Verdict: the overlay is build-ready — 10 line-pair (1,1) joint marginals
per key capture 93.8% of the workload's measured +2 looseness (~5.9% of
workload boards). Storage: +10 B/key; the cwd_mm cell grows 29 -> 39 B,
pushing the artifact slot to 64 B (8.6 GiB) — or the pair bytes ship as a
separate 2^27 x 16 B sidecar (2.1 GiB) probed only on multi-demand axes.
Engine: surcharge = max(single-line-max, demanded-pair values); also lifts
the un-refined axis term inside every LM2 branch. Build = ten
cwd_single-style constrained sweeps.

Overlay/LM2 stacking probe (harness ovlunion, 507 workload boards,
production single-line-max baseline):
  LM2 alone 307 (60.6%) | overlay alone 30 (5.9%) | combined 325 (64.1%)
  combination-only boards 18 (+3.6 pts); combined >= +4 on 11 boards.
The overlay's boards are ANTI-correlated with LM2's certified set (18/30 =
60% outside vs 39% under independence) — it stacks better than
independence predicts. By the calibration slope (+15 pts ~ x1.25 nodes),
+3.6 pts ~ x1.05 nodes est. — a few percent of wall.

CAVEAT recorded per methodology review: all certification numbers are
conditioned on the 507-board sample = k8 PRUNE EVENTS. They measure
replication of one mechanism's prunes, not total achievable cut; a
mechanism pruning boards k8 misses is invisible here. The unconditioned
fix (cheap, if pursued): dump slack-0 cWD-survivor boards from a live run
(AUTOPSY keyed on survival, not k8-pruning) and re-run the probes.

Joint-form LM pricing: closed by this data — the overlay recovers the
demand-pricing component (certU 64.1% vs joint-form 74.6% leaves ~10 pts),
and the residual is the zero-demand refined term x demand interaction,
whose only table realization is demand-dimensioned LM tables (~x32,
~70 GB). At the measured saturation (certification points now buy ~1/4 of
their early node value), that residual projects to low-single-digit
percent of nodes: not worth the table family. Overlay stays the one
build-ready node-side upgrade: ~1.05x est., ~a day of build+A/B.

Unconditioned re-sample (engine SURVIVOR dump: every 26M-th cWD-survivor
of a base sequential exhaust-146, 555 boards — conditioned on nothing but
survival; k8 never consulted). Probes re-run:

                          k8-conditioned (507)   unconditioned (555)
  LM2 table cert                58.2%                 58.6%
  LM2 (prod baseline)           60.6%                 60.5%
  overlay alone                  5.9%                  2.9%
  overlay stacking on LM2       +3.6 pts              +0.9 pts (5 boards)
  >= +4 boards                    11                    22 (all LM2's own)

Predictions graded: "LM2 drops under de-conditioning" WRONG — identical;
"overlay holds ~6%" WRONG — halved. The k8 sample was unbiased for LM2 but
inflated the overlay ~2x (k8 prune events over-represent multi-demand
boards). LM2's unconditioned +4 rate is 4.0% (22/555), double the
conditioned 2.0%.

VERDICT REVERSED: the pairwise-(1,1) overlay drops from build-ready
(~1.05x est.) to not-worth-the-day (~1.01-1.02x est. from +0.9 pts at
measured saturation). Recorded, not built. The sampling hook
(FLAT_SURVIVOR_DUMP, stats build) is the standing instrument for future
certification probes — all future probe numbers should use unconditioned
survivor samples.

================================================================================
EXHAUST-148 CONFIRMATION (--parallel --lm2, AC power, canaries in-run clean)
================================================================================

  threshold 144:     216,158,191 nodes    1.15 s   (canary: exact)
  threshold 146:   8,975,456,521 nodes   57.80 s   (canary: exact, best wall)
  threshold 148: 288,304,558,446 nodes 2084.48 s  (138.31 Mn/s)

Pre-registered predictions graded: nodes 282-289 B -> 288.3 B RIGHT;
wall ~2000 s -> 2084 s RIGHT. LM2 cut vs base (577,673,506,215):
2.004x — the trend 1.954 (@144) -> 1.980 (@146) -> 2.004 (@148) is
confirmed slowly GROWING with depth. Wall 2084.48 s vs the 148 dead heat
(cWD-only 3256.50 s / k8 3254.78 s): 1.56x — first configuration to win
the 148 race. k8 still out-prunes on nodes (193.3 B, 2.99x) but its
59.4 Mn/s throughput loses the race to LM2's 138.3 Mn/s.

Exhausting 148 proves R >= 150. The R >= 152 run (exhaust-150,
~35x growth -> ~10 T nodes, ~20 h at this rate) is GO on this
configuration whenever scheduled.

================================================================================
R >= 152 PROVED (exhaust-150, --parallel --lm2, 2026-08-01, AC, full log
data/r_lm2_exhaust150.log)
================================================================================

  threshold 144:         216,158,191 nodes      1.15 s  (canary exact)
  threshold 146:       8,975,456,521 nodes     57.66 s  (canary exact)
  threshold 148:     288,304,558,446 nodes   2099.32 s  (canary exact)
  threshold 150:   7,408,277,152,495 nodes  59071.36 s  (125.41 Mn/s)

  total 7,705,773,325,653 nodes in 61,229.49 s (17.0 h); setup 904 ms
  (mmap artifacts). Output: "Lower bound: depth >= 152".

Growth factor 148->150: 25.7x — the declining trend (41.5 -> 32.1 -> 25.7)
continued; the revised 7-9 T estimate was right, the original 10 T
conservative. Throughput eased 138 -> 125 Mn/s at depth (front-cache
working-set growth, as the hit-rate trend predicted).

For scale: cWD-only at 150 projects ~15 T nodes / ~24 h; the k8 2-tier
~3.6 T / ~17 h (crossover-near-150, as estimated). LM2 got there in
16.4 h on 2.2 GB of tables. Next threshold (exhaust-152, proves >= 154)
projects ~150-190 T at factor ~20-25 -> ~2-3 weeks on this box with LM2
alone; the k8-on-lm2 cascade decision (crossover estimated at ~152) awaits
the k8 survivor-sample certification.

k8 certification on the unconditioned survivor sample (flat.rs ignored
test k8_certification_survivor_sample; certL cross-check = 336/555 exact):

  certK (k8)  353 (63.6%)   k8 advantage: +2 x141, +4 x129, +6 x56, +8+ x27
  certL (lm2) 336 (60.5%)
  both 255 | k8-only 98 (17.7%) | lm2-only 81 (14.6%) | neither 121 (21.8%)
  union prunes 78.2% of the frontier; k8's prunes are DEEP (+4 or more on
  60% of its events) where lm2's are almost all +2 — the measured mechanism
  behind k8's faster-declining growth factor.

Cascade estimate refreshed: surviving-frontier share 21.8% vs lm2's 39.5%
and k8-alone's 36.4% brackets cascade@148 nodes in [116 B, 160 B]
(k8-relative vs lm2-relative translation disagree because board shares
ignore subtree nesting). At an assumed ~55-58 Mn/s cascade throughput that
is wall 2000-2900 s vs lm2's 2084 s — PAR TO 1.4x WORSE at 148, tipping
toward the cascade at 150+ as k8's deep advantages compound. Decision
needs two direct measurements, both cheap once the cascade is wired:
  1. cascade exhaust-146 (minutes): exact union node count;
  2. budgeted run: true cascade throughput (k8 consult cost at
     lm2-survivors).

Pairwise-matching probe (harness pairprobe: Korf-Taylor 2-tile additive
PDBs, all 276 pairs, 49 ms build; exact max-surplus matching per board;
555-board unconditioned survivor sample, production baseline):
  certP 7/555 (1.26%); mean matched surplus 1.6; mean (PM - h0p) -3.0;
  mean (h0p - ΣMD) = 4.6.
Predictions graded: certP "≈0" direction right (7 boards); surplus scale
right; the WD-MD gap prediction (15-30) BADLY WRONG at 4.6 — the finding:
at the slack-0 frontier, cWD's advantage over Manhattan COLLAPSES (32 at
the R root -> 4.6 at survivors). Frontier boards are by construction the
boards every heuristic underestimates; all heuristics compress toward MD
there, which is why modest joint surpluses (mean 1.6) land within 3 of
production and occasionally poke above. Verdict: pairwise-matching as a
max-component prunes ~1.3% of frontier boards — overlay-scale value at
higher per-node cost (matching); not worth engineering. The k8 cascade
remains the only joint-2D route with measured frontier reach (17.7%
k8-only).

Triple-packing probe (harness tripleprobe: all 2,024 3-tile additive PDBs,
32 MB, 11.5 s build; exact mixed pair/triple surplus packing; 555-board
unconditioned survivor sample, production baseline):
  certT 118/555 (21.26%)  |  pairs were 7/555 (1.26%)
  mean matched surplus 3.3 (pairs 1.6); mean (PM3 - h0p) -1.3 (pairs -3.0).
Prediction (certT 2-6%) BADLY WRONG — the tuple ladder is NOT flat:
third-order blank-corridor interference is large at the frontier and
survives compression. Biggest surprise since the +2 detector. Union with
LM2 running; 4-tuple derivative (~10,626 tables, ~3-4 GB, ~1-2 h build)
now genuinely live.

Tripleprobe union: certT 118 (21.26%) vs certL 336 (60.54%); both 93 —
79% of triple coverage NESTS inside LM2's set (third-order interference
concentrates near the corner regions LM2 already prunes). Triple-only
25/555 = 4.50 pts incremental — 5x the demoted overlay, 1/4 of k8-only
(17.7%). By the calibration slope ~ x1.06-1.08 nodes IF the full exact
packing were realized per node (engine versions would approximate down).
Open rung: the 4-tuple derivative (mean surplus grew 1.6 -> 3.3 per rung;
10,626 tables, ~3-4 GB, ~15 min threaded build) — measures whether the
ladder's LM2-incremental grows toward k8-only territory or saturates.

Dual lookups: KILLED at the soundness gate. d(dual) != d(s) on 5+/300
random boards (d(dual) LARGER by 4-12 — inadmissible as a lookup). The
Zahavi-Felner-Holte duality preconditions (location-based operators) fail
for blank-relative sliding-tile moves; my recall that the dual-lookups
paper covered the tile puzzle was wrong. Untried variant: blank-canonical
duality (blank at goal cell). BPMX shelved with it (no inconsistent
component). Run terminated before the full 300 count — verdict sealed by
construction (any overestimate disqualifies).

k8 per-group surplus breakdown re-examined (flat.rs test
k8_group_breakdown_survivor_sample, unconditioned 555 sample, winning
sigma-view, surplus = PDB value - group MD). Groups: a = top-left
[1,2,3,4,6,7,8,9]; b = right column + bottom-right [5,10,14,15,19,20,
23,24] — LM2's entire cast plus column neighbors; c = mid/bottom-left
[11,12,13,16,17,18,21,22].
  all boards:  a 15.3 / b 49.2 / c 35.4  (earlier 52% b-dominance robust)
  certK:       a 18.2 / b 45.6 / c 36.2
  k8-only:     a 21.3 / b 41.6 / c 37.1
Verdict: still true directionally, ERODED relative to LM2 — the residual
k8-only advantage is more evenly spread (b 42%, c 37%, a 21%). Two
implications: (1) a single-group "cheap k8" tier (~11 GB, one PDB) would
carry well under half of the k8-only reach — the cascade needs all three
groups; (2) consistent with the tuple ladder's 79% LM2-nesting: joint
content in b's region is largely already priced by LM2; what k8 uniquely
adds is distributed joint structure, not one region's.

CORRECTION (user methodology challenge): "no regional concentration" was
overclaimed from aggregate shares. Proper criticality test (replace each
group's value with its MD per view, re-max, ask if the +2 survives) on the
98 k8-only boards: b necessary on 67%, c on 52%, a on 29% — REAL
concentration in necessity. But 48% of boards need >= 2 groups jointly
(21% all three), so the no-cheap-partial-k8 verdict stands via JOINTNESS,
not via even spread. Single-group SUFFICIENCY (b + MD-of-rest >= +2)
remains unmeasured; the 48% multi-critical floor bounds any single-group
tier's reach at <= 52% of k8-only regardless.

k6/k7 zPDB frontier probe (flat.rs test k6k7_certification_survivor_sample;
the June-built pdb24_{a..d}.zbin 6-6-6-6 at 88 MB total and
pdb24_k7_{a..d} 7-7-7-3 at 1.5 GB; no build needed — 14 s probe):
  certL 60.5% | certK6 42.0%, K6-ONLY BEYOND LM2 52/555 = 9.37%,
  advantages 75x+4? no: adv hist (>=2): [127 +2, 75 +4, 24 +6, 7 +8+]
  certK7 24.7%, k7-only 3.60% — WORSE than k6: the 3-tile runt group
  [19,23,24] wastes a quarter of the partition; group-size symmetry beats
  group size. k8 reference: 63.6% / 17.7%.
Predictions under-shot again (42 vs 25-40; 9.4 vs 4-9 band top). Decision
per pre-registration (>=8% at ~free consult cost): k6 is now the LEADING
cascade-tier candidate — 88 MB fully RAM-resident, consults ~8 cheap
lookups (or a 4 GiB positional re-layout for shift-only indexing), reach
9.4% beyond LM2 = calibration-slope est. ~x1.10-1.15 nodes. Next: wire a
lazy k6 tier on lm2 (mirror of the k8 tier) + the two direct measurements
(cascade exhaust-146 nodes, budgeted throughput). The k8 cascade
(17.7% reach at 59 Mn/s economics) is now the fallback, not the default.
Also banked: threaded harness k6probe mode builds arbitrary blank-free
partitions in ~25 min — the partition-design probe instrument.

================================================================================
k6 CASCADE TIER: ranked vs positional layout (2026-08-02)
================================================================================

Tier wired above LM2 (consulted only at LM2-survivors). Node counts are
layout-independent and identical across every arm:
  exhaust-144  205,119,447  (lm2 alone 216,158,191 → cut 1.054x)
  exhaust-146  7,802,326,121 (lm2 alone 8,975,456,521 → cut 1.150x)

Profile of the ranked tier (sample, sequential exhaust-146): the tier is
36.1% of runtime — ZpdbLayout::rank 23.6% (65% OF THE TIER), k6_child body
6.4%, memmove of the 8-ProjectedState slot 5.9%.

Consult-stream locality (feature k6-locality, 49.7 M probes): a front cache
keyed on the exact abstract state hits 77.9% @2^14, 86.3% @2^16, 92.1%
@2^18, 95.5% @2^20, 96.9% @2^22. Positional address stream touches only
362 K distinct 128 B lines (46 MB) over 43.5 K pages; consecutive-probe
same-line 0.12% (no spatial locality — probes alternate group and σ-view).

POSITIONAL LAYOUT (src/puzzle24/search/k6pos.rs): index = blank·25^6 +
Σ p_j·25^(5-j) over the six pattern cells; stores the SAME 1-bit
differential codec, so 763 MB/pattern (2.9 GB total) instead of 24.4 GB for
u8 values. Maps built FROM SCRATCH by their own zero-aware BFS (cost-1
pattern-tile moves, cost-0 blank walks, layer closed under cost-0 before
cost-1 expansion), 43 s each, depth 35. Cross-check vs the ranked zPDBs:
10,151 cost-1 probes match exactly.
  BUILD BUG WORTH REMEMBERING: seeding the blank at every free cell (rather
  than only its goal cell 24) collapses the blank-REGION distinction the
  zero-aware abstraction rests on. Pattern d's goal walls cell 24 in between
  tiles 20 and 24, so its region is isolated; the cross-check caught it
  (probe 4 vs cold 6) and zbuild::build_zpdb already carried a comment
  warning of exactly this.
Runtime: 40-byte arena slot (8 u32 indices + 8 u8 values) replaces eight
ProjectedStates; one multiply-add per σ-view updates the moved tile's group
index; the blank digit is applied inside the probe. rank and memmove gone.

Canary-bracketed A/B @146 (canaries lm2 57.60 / 58.62 s):
  lm2 alone            57.60 s   155.8 Mn/s      —
  + k6 ranked          90.46 s    86.3 Mn/s   1.57x slower than lm2
  + k6 positional      75.31 s   103.6 Mn/s   1.31x slower than lm2
Positional buys 1.20x throughput over ranked; the tier's toll falls
1.76x -> 1.50x.

Joint cache sweep @146 (idle box, (21,18) repeated as its own canary:
75.44 / 74.83 s):
  lm2=21 k6=18  75.44 / 74.83 s   <- best
  lm2=19 k6=18  74.74 s           <- tied within noise (frees 384 MB)
  lm2=19 k6=20  79.10 s
  lm2=21 k6=20  79.27 s
  lm2=21 k6=22  80.03 s
Finding: ENLARGING the k6 cache HURTS (2^18 -> 2^20 costs 5-6%) even though
its hit rate rises 92.1% -> 95.5% — the k8 memo lesson again, now with a
mechanism: at 2^18 (4 MB/worker) the cache is small enough to stay
effective, and past that the extra capacity costs more in footprint than
the 3.4 pp of avoided map probes returns. LM2 19 vs 21 is a wash with k6
active (was 1.5% apart alone), so the 64->16 MB/worker reduction is free if
the memory is wanted elsewhere. Defaults unchanged (lm2=21, k6=18).

Standing: the tier still LOSES at 146 (cut 1.150x vs toll 1.50x). The cut
grows with depth (1.054 -> 1.150) while the toll is fixed, so the measured
crossover is near threshold 150 and the tier should pay at 152 — the next
campaign's threshold. Next measurement: exhaust-148 with --lm2 --k6pos vs
--lm2 alone, which replaces the extrapolated 1.27x cut with a measured one.

LM2 cache size re-decided on hit rates (stats build, 144+146, --lm2 --k6pos;
timings meaningless under the shared atomics, counts exact):
  bits 19 (16 MB/worker): 12,040,283,593 hits / 31,097,763 misses = 99.742%
  bits 21 (64 MB/worker): 12,061,045,888 hits / 10,335,468 misses = 99.914%
  (k6 cache unchanged either way: 89.489% vs 89.493% — in situ, vs 92.1%
   simulated at 144 only; the deeper working set costs ~2.6 pp.)
4x the memory buys 0.17 pp of hit rate: 21 M fewer misses out of 12.07 B
probes. At the measured wall (19 and 21 tied at 146: 74.74 vs 74.83/75.44 s)
that is not worth 48 MB/worker = 384 MB. DEFAULT LOWERED 21 -> 19 per user
preference (smaller wins ties). The depth argument for the larger cache —
fixed cost, benefit growing as hit rates decay — survives only as a 0.17 pp
reserve, which would have to grow ~10x to matter; if a deep run ever shows
the LM2 miss rate climbing, FLAT_LM2_CACHE_BITS=21 restores it in one env
var. Node counts re-verified after the change (216,158,191 / 205,119,447).

Why the k6 tier does not parallelise (user question: cWD/LM2 have BIGGER
maps and scale fine). It is miss RATE, not map size:
  tier   map     front-cache hit   probes reaching the map
  cWD    4.0 GB      99.665%             0.34%
  LM2    4.0 GB      99.742%             0.26%
  k6     2.9 GB      89.489%            10.51%
k6 sends ~35x more traffic to DRAM per probe, and that traffic is what fails
to scale: sequential toll is only 1.27x (26.31 -> 20.74 Mn/s @144), but 8-way
scaling falls from LM2's 7.25x to 4.63x, so the parallel toll is 1.50x @146.
Three mechanisms behind the low reuse: (1) k6 keys are exact cells of six
named tiles + blank (2.42 G states x 8 streams) vs cWD's per-line token
counts (65.6 M total); (2) the blank is in the key and moves on EVERY move,
while a cWD axis key only changes when a move crosses that axis; (3) the key
is finer than the value's dependency — the zero-aware value varies by blank
REGION, not blank cell, so one value is cached under ~a dozen keys.

Two cheaper-probe variants measured on the 555-board survivor sample:
  single σ-view (halves probes):  certK6 31.5%, beyond-LM2 7.03% (vs 9.37%)
  blank-free (25^6 = 30.5 MB/pattern, 122 MB total, fixes (2) and (3)):
      certK6 17.1%, BEYOND-LM2 1.98% (vs 9.37%), advantages [66x+2, 23x+4,
      5x+6, 1x+8]
Blank-free loses 79% of the tier's unique reach — blank-awareness is where
k6's value lives, so the cheap-map route is dead as tested. Single-view keeps
75% of reach for half the traffic and remains the one live cost lever.
